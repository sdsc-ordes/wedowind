# JSON Schema (optional)

Place schemas here for log records or register payloads, for example:

- `publish_event.schema.json`
- `discrepancy_entry.schema.json`

Validate from Python with the `jsonschema` library (project dependency in `pyproject.toml`, installed via `uv sync`).
